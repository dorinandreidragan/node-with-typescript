import fs from "fs";

describe("Get", () => {
  
  // f1
  // README.md
  it("should ", () => {
    expect(4).toBe(4);
  });
});

function createFile(name: string): void {
  fs.writeFileSync(name, "");
}
